apiVersion: v1
kind: Service
metadata:
  name: {{ include "rounds.fullname" . }}
  labels:
    {{- include "rounds.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "rounds.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
