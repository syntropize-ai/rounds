{
  "id": "missing-source-as-ruled-out",
  "note": "An investigation that never had change data and tried to record that absence as an eliminated deployment. The tool WAS called and answered 'no connector configured', so a plain call-counter cannot catch this; the ledger records whether the source answered.",
  "expect": "rejected-at-record",
  "reads": {
    "metric": 1,
    "change": 1
  },
  "checks": [
    {
      "hypothesis": "the api gateway is returning errors",
      "signalType": "metric",
      "tool": "metrics_query",
      "query": "sum(rate(http_requests_total{status=~\"5..\"}[5m]))",
      "result": "error rate is 1.2/s and rising over the last 30m",
      "interpretation": "Confirms an elevated error rate on the api gateway.",
      "status": "supported",
      "scope": {
        "timeWindow": "last 30m",
        "affected": "api-gateway"
      }
    },
    {
      "hypothesis": "a recent deployment caused the regression",
      "signalType": "change",
      "tool": "changes_list_recent",
      "query": "api-gateway last 120m",
      "result": "No change-event connector configured. Call connectors_list to see available sources.",
      "interpretation": "Rules out a recent rollout.",
      "status": "ruled_out",
      "scope": {
        "timeWindow": "last 120m",
        "affected": "api-gateway"
      }
    }
  ],
  "claim": {
    "summary": "the api gateway is failing for reasons unrelated to any deployment",
    "rootCause": {
      "status": "confirmed",
      "object": "api-gateway",
      "field": "error rate",
      "cause": "elevated 5xx unrelated to any recent rollout"
    },
    "confidence": 0.85,
    "evidenceRefs": [
      "check_1",
      "check_2"
    ],
    "ruledOut": [
      "recent deployment"
    ],
    "nextAction": "investigate the api gateway further",
    "validationMethod": "verify the error rate returns to baseline"
  },
  "why": "The change source was never configured, so it eliminated nothing. Recording that absence as a ruled-out deployment retires the likeliest cause of most incidents while satisfying the gate's 'a competing explanation was ruled out' requirement — a confident wrong answer wearing an evidence trail. The read ledger now knows the source never answered and refuses any status but inconclusive.",
  "unavailable": [
    "change"
  ]
}
